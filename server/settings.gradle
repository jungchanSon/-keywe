rootProject.name = 'kiosk'

include(
        "user-service",
        "store-service",
        "infra-gateway",
        "infra-config",
        "common",
        "common-token",
        "common-http",
        "swagger"
)
