rootProject.name = 'kiosk'

include(
        "user-service",
        "store-service",
        "remote-service",
        "infra-gateway",
        "infra-config",
        "common",
        "common-token",
        "common-http"
)
