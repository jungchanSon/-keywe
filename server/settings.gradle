rootProject.name = 'kiosk'

include(
        "user-service",
        "store-service",
        "order-service",
        "remote-service",
        "infra-gateway",
        "infra-config",
        "common",
        "common-token",
        "common-http",
        "swagger"
)
